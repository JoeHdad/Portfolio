# دليل HumanEdit - مشروع Portfolio
## دليل شامل للعمل مع المشروع وإدارة Webhooks والمفاتيح السرية

---

## 🎯 نظرة عامة على المشروع

هذا المشروع عبارة عن Portfolio احترافي مبني بـ React و TypeScript مع نظام عدّ الزوار التفاعلي. المشروع يستخدم Netlify Functions كـ backend لإدارة البيانات.

---

## 📁 هيكل الملفات المهمة

### 🔧 ملفات الإعدادات الأساسية
```
Portfolio/
├── .env.example                    # نموذج المتغيرات البيئية
├── netlify.toml                    # إعدادات Netlify
├── package.json                    # إعدادات المشروع والـ dependencies
└── vite.config.ts                  # إعدادات Vite
```

### 🌐 ملفات Webhooks والـ API
```
Portfolio/netlify/functions/
├── get-visitors.ts                 # Webhook لجلب عدد الزوار
└── increment-visitors.ts           # Webhook لزيادة عدد الزوار
```

### ⚛️ ملفات React Components
```
Portfolio/src/components/
├── VisitorCounter.tsx              # مكون عدّ الزوار (الأهم!)
└── [other-components...]           # باقي مكونات الموقع
```

---

## 🔗 Webhooks والإEndpoints

### Webhooks المتاحة:
1. **جلب عدد الزوار**: `/.netlify/functions/get-visitors`
   - **الطريقة**: POST
   - **الغرض**: جلب العدد الحالي للزوار
   - **الملف**: `netlify/functions/get-visitors.ts`

2. **زيادة عدد الزوار**: `/.netlify/functions/increment-visitors`
   - **الطريقة**: POST
   - **الغرض**: زيادة عداد الزوار
   - **الملف**: `netlify/functions/increment-visitors.ts`

---

## 🔐 إدارة المفاتيح السرية (API Keys)

### 📍 مكان تخزين المفاتيح:
- **الملف**: `.env.example` (النموذج)
- **الملف الفعلي**: `.env.local` (يجب إنشاؤه)

### 🔑 المفاتيح المطلوبة:
```bash
# إعدادات Vercel KV للعدّاد
KV_URL=rediss://default:your_token@your_redis_url:6379
KV_REST_API_URL=https://your_rest_api_url
KV_REST_API_TOKEN=your_rest_api_token
KV_REST_API_READ_ONLY_TOKEN=your_read_only_token
REDIS_URL=rediss://default:your_token@your_redis_url:6379
```

### 🛡️ أمان المفاتيح:
- ✅ **المفاتيح محمية**: لا يتم رفعها إلى Git (محمية في .gitignore)
- ✅ **متغيرات بيئية**: مخزنة كمتغيرات Netlify البيئية
- ⚠️ **يجب عليك**: إضافة المفاتيح الفعلية في Netlify dashboard

---

## ⚙️ خطوات الإعداد

### 1. إعداد متغيرات Netlify البيئية:
```bash
# انتقل إلى Netlify Dashboard → Site Settings → Environment Variables
# أضف المفاتيح التالية:
KV_REST_API_URL=your_actual_url
KV_REST_API_TOKEN=your_actual_token
```

### 2. تشغيل المشروع محلياً:
```bash
cd Portfolio
npm install
npm run dev
```

### 3. نشر المشروع:
```bash
npm run build
# سيتم نشر الملفات في مجلد dist/
```

---

## 🔍 فحص Webhooks والتأكد من صحتها

### 📊 طريقة فحص Webhooks:

1. **فحص الكود**: 
   - راجع `netlify/functions/get-visitors.ts` (السطور 4-7)
   - راجع `netlify/functions/increment-visitors.ts` (السطور 4-7)

2. **فحص اتصالات API في المتصفح**:
   - افتح Developer Tools (F12)
   - انتقل إلى تبويب Network
   - اعرض الموقع وسيتم عرض الطلبات للـ endpoints

3. **اختبار يدوي**:
```bash
# اختبار جلب الزوار
curl -X POST https://your-site.netlify.app/.netlify/functions/get-visitors

# اختبار زيادة الزوار  
curl -X POST https://your-site.netlify.app/.netlify/functions/increment-visitors
```

---

## 🎯 المكونات المهمة للعمل معها

### 1. **VisitorCounter.tsx** - مكون عدّ الزوار:
- **المسار**: `src/components/VisitorCounter.tsx`
- **السطر 14-15**: إعدادات API endpoints
- **السطر 17-62**: دالة `fetchVisitorCount()` للتعامل مع الـ API
- **السطر 82-115**: منطق زيادة العدّاد

### 2. **مكونات Netlify Functions**:
- **السطور 4-7 في كل ملف**: إعداد اتصال KV
- **السطور 10-24**: معالجة CORS headers
- **السطور 26-40**: منطق العمليات (جلب/زيادة)

---

## 🚀 التشغيل والتطوير

### أوامر مفيدة:
```bash
# تشغيل المشروع للتطوير
npm run dev

# بناء المشروع للإنتاج
npm run build

# فحص الأخطاء
npm run lint

# معاينة البناء
npm run preview
```

### منافذ التشغيل:
- **Development**: `http://localhost:5173` أو `http://localhost:8080`
- **Production**: Domain الخاص بك على Netlify

---

## 🔧 تخصيص المشروع

### تغيير عدد الزوار الأولي:
- في `VisitorCounter.tsx` السطر 56: `setVisitorCount(fallbackCount || 1000);`

### تخصيص الرسائل:
- في `get-visitors.ts` السطر 51: `'Failed to get visitor count'`
- في `increment-visitors.ts` السطر 52: `'Failed to increment visitor count'`

### تعديل تكرار التحديث:
- في `VisitorCounter.tsx` السطر 98: `Math.random() * 15000 + 15000`

---

## ✅ قائمة فحص للـ Production

- [ ] إضافة المفاتيح السرية في Netlify dashboard
- [ ] فحص عمل Webhooks في البيئة المباشرة
- [ ] اختبار عدّ الزوار على الموقع
- [ ] التأكد من عمل CORS headers
- [ ] فحص رسائل الخطأ في حالة فشل API

---

## 🆘 استكشاف الأخطاء

### مشاكل شائعة:

1. **API Keys مفقودة**:
   - تأكد من إضافة المتغيرات البيئية في Netlify
   - راجع `.env.example` للتأكد من جميع المفاتيح المطلوبة

2. **Webhooks لا تعمل**:
   - فحص Network tab في DevTools
   - التأكد من أن Functions منشورة على Netlify

3. **عداد الزوار لا يظهر**:
   - فحص console للأخطاء
   - التأكد من اتصال الإنترنت
   - التحقق من KV connection

---

## 📞 التواصل والدعم

إذا واجهت أي مشاكل:
1. راجع logs في Netlify Dashboard
2. فحص Console في المتصفح للأخطاء
3. تأكد من صحة API Keys والـ URLs

---

**ملاحظة**: هذا المشروع يستخدم أفضل الممارسات في الأمان والتطوير، جميع المفاتيح السرية محمية والإعدادات مُحسّنة للأداء.